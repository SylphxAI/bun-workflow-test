name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    permissions:
      contents: write
      pull-requests: write
    uses: SylphxAI/.github/.github/workflows/release.yml@feat/release-delegates-to-changesets
    secrets: inherit
